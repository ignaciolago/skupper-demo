---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: skupper
  namespace: openshift-gitops
spec:
  generators:
  - clusters: {}
  template:
    metadata:
      name: skupper-{{name}}
    spec:
      project: default
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
      source:
        repoURL: https://github.com/ignaciolago/skupper-demo.git
        targetRevision: main
        path: resources/skupper
      destination:
        server: https://kubernetes.default.svc